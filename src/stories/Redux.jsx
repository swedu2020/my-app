import React from 'react';
import PropTypes from 'prop-types';
import ReduxApp from '../07/SearchFilterReduxApp';
export const Redux = () => {
    return(
        <React.Fragment>
            <ReduxApp />
        </React.Fragment>
    );  
};
Redux.propTypes = {

};
Redux.defaultProps = {

}